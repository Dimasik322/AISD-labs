//#include <vector/vector.h>
#include <sort/sort.h>
#include <iostream>

int main() {
	srand(unsigned(time(0)));
	compare_sort_sorted<int>(2000, true);
	// if (i < j) {
	//auto array = random_array<int>(10);
	//cout << array;
	//auto stat = quick_sort(array);
	//cout << array;
	//auto stat_shell = quick_sort(array);
	//cout << array;

	//cout << stat_bubble;
	//compare_sort_random<int>(1000);
	return 0;
}